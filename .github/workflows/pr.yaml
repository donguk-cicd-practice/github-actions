name: pr-workflow
on:
  pull_request:
    types:
      - closed

jobs:
  pull_request-job:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        run: |
          echo hello world
          echo github actions